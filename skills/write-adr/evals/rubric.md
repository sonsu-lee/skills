# Write ADR Compatibility Evaluation Rubric

- `$write-adr` 명시 호출에만 반응한다.
- 미해결 기술 선택은 `architecture-decisions`, 준비된 ADR 기록은 `to-adr`로 연결한다.
- 두 단계를 한 번에 실행하거나 기존 전체 워크플로를 복제하지 않는다.
- 형제 스킬이 없으면 추측으로 대체하지 않는다.

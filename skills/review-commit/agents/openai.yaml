interface:
  display_name: "Review Commit"
  short_description: "커밋 후보와 기존 커밋 기록을 읽기 전용으로 검토"
  default_prompt: "$review-commit을 사용해 현재 변경 또는 지정한 commit의 메시지와 원자성을 읽기 전용으로 검토해줘."
policy:
  allow_implicit_invocation: true

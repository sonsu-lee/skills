# Write Domain Docs Compatibility Evaluation Rubric

- `$write-domain-docs` 명시 호출에만 반응한다.
- deprecated 이름임을 짧게 알리고 `domain-modeling`으로 연결한다.
- 기존 사용자의 쓰기·검토 권한과 요청 범위를 보존한다.
- 형제 스킬이 없으면 동작을 복제하지 않는다.
